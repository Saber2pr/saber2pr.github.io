name: PPT
on:
  workflow_dispatch:
    inputs:
      markdown:
        description: 'markdown path'
        required: true
        
jobs:
  Deploy-Pages:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4

      - name: Install Deps
        run: rm package.json && yarn add markppt
       
      - name: Create PPT
        run: yarn markppt ${{github.event.inputs.markdown}}

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./blog
